workflows:
  expo-apk:
    name: Expo APK Build
    environment:
      node: latest
      groups:
        - expo

    scripts:
      - npm install
      - npm install -g eas-cli
      - eas build -p android --profile preview --non-interactive

    artifacts:
      - "**/*.apk"
